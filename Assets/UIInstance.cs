using System;
using UnityEngine;

public class UIInstance : MonoBehaviour
{
    public static UIInstance Instance;

    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
